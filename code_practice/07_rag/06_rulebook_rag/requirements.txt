# Verified against the versions this project was built and measured on.
# Lower bounds, not pins - the code uses no version-specific behaviour beyond
# `output_config` structured outputs, which needs a reasonably current anthropic SDK.

anthropic>=0.121.0          # output_config.format (structured outputs)
sentence-transformers>=5.5.1
torch>=2.12.0               # CPU build is sufficient; no GPU path in this project
numpy>=2.4.6
scikit-learn>=1.8.0
pymupdf>=1.27.2             # imported as `fitz`
pyyaml>=6.0.3
requests>=2.34.2

# Notes
#   - On macOS, plain `pip install torch` is the CPU build. The
#     --index-url .../whl/cpu form hosts Linux/Windows wheels and is not needed here.
#   - Two models download on first use into ~/.cache/huggingface (~220 MB total):
#       BAAI/bge-small-en-v1.5                    bi-encoder,   384-dim
#       cross-encoder/ms-marco-MiniLM-L-6-v2      cross-encoder reranker
#     Everything after that first download runs offline; `cli doctor` verifies both
#     load from cache.
