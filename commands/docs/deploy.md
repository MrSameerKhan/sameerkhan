# Conceptual Difference

| Term   | Meaning |
|--------|----------|
| **Build**  | The process of packaging your code into a portable, reproducible artifact (e.g., wheel, Docker image, zip). |
| **Deploy** | The process of taking that built artifact and running it somewhere — a server, a cloud service, or an endpoint — so that users or systems can access it. |
