#Start Docker Desktop #Jenkins runs inside Docker, so Docker must be running
1. Open Docker Desktop
1. docker info


# Start Jenkins Container
2. docker start jenkins

#Open Jenkins UI
http://localhost:8080


#Verify Jenkins is Running
docker ps
